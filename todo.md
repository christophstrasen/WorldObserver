@TODO

- Investigate aggregate distinct convenience helpers (LQR): allow `count` to be `distinct=true` when `path` already identifies the distinct key, so examples don't need a verbose `distinctFn` like `return row.zombie.outfitName`.
